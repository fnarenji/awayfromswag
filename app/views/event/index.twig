{% extends "layout/default" %}
{% block content %}
    <article>
        <h1>Évènements</h1>
        {% if events is empty %}
        <p>Il n'y a pas d'évènements.</p>
        {% else %}
        <ul id="events">
            {% for event in events %}
            <li>
                <a href="/event/show/{{ event.id }}"><h1>{{ event.name }}</h1></a>
                <h2>{{ event.eventtime }}</h2>
                <h2>Prix : {{ event.money }}€</h2>
                <p>
                    {{ event.description }}
                </p>
            </li>
            {% endfor %}
        </ul>
        {% endif %}
    </article>
{% endblock %}